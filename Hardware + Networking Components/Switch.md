Aggregates multiple devices to a network; regenerates an electrical signal (data frames) and sends it to the receiving nodes instead of broadcasting; identifies the MAC address of each computer.

See: [[Hub]]
